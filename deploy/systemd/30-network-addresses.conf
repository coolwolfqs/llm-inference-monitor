[Service]
# Go's net.Interfaces and gopsutil resolve interface addresses over netlink.
# Keep the service hardened while allowing read-only network metadata queries.
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX AF_NETLINK
